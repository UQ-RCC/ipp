'use strict';

describe('strudelWeb.job-list module', function() {

  beforeEach(module('strudelWeb.job-list'));

  describe('job-list controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var experimentManagerCtrl = $controller('JobListCtrl');
      expect(experimentManagerCtrl).toBeDefined();
    }));

  });
});
